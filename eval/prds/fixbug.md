The file `test_calc.py` in this directory is failing. Run it to see the failure, then fix the bug in `calc.py` so that all its assertions pass.

Rules:
- Do NOT modify `test_calc.py` — the tests are correct; the bug is in `calc.py`.
- Change only what is necessary in `calc.py`.
- Do not create any new files.
- Verify by running `python3 test_calc.py` and confirm it prints `all tests passed`.
