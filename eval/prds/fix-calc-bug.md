# Fix the bug in calc.py

`test_calc.py` fails because `calc.py` has a bug. Run the test, find the bug in
`calc.py`, and fix it so the test passes.

Fix only `calc.py`. Do NOT modify `test_calc.py` — the test is correct; the code
is wrong.
