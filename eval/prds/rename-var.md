# Rename a variable in greet.py

In `greet.py`, rename the local variable `msg` to `greeting`, in place. Update
every use of it so the file still runs and `greet("World")` still returns
`"Hello, World!"`.

Change only `greet.py`. Do not rename anything else or create new files.
