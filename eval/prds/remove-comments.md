# Remove comments from area.c

The file `area.c` contains C code with comments. Remove ALL comments from
`area.c` — both the `//` line comments and the `/* ... */` block comments —
editing the file in place.

Keep every line of actual code exactly as it is; only the comments should be
gone. The file must still compile.

Do not touch any other file in the directory.
