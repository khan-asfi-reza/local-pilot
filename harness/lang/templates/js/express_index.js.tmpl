const express = require("express");
{{if .has_postgres}}const { pool } = require("./db");{{end}}

const app = express();
app.use(express.json());

app.get("/", (req, res) => {
  res.json({ status: "ok" });
});

const port = process.env.PORT || 3000;
app.listen(port, () => console.log(`listening on ${port}`));
